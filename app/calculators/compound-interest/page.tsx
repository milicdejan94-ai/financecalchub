import CompoundInterestClient from './CompoundInterestClient';

export const metadata = {
  title: 'Compound Interest Calculator | Estimate Investment Growth',
  description:
    'Free compound interest calculator. Estimate how savings or investments may grow over time with regular contributions.',
};

export default function CompoundInterestPage() {
  return <CompoundInterestClient />;
}